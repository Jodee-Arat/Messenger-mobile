query GetSecretMessages($chatId: String!) {
	getSecretMessages(chatId: $chatId) {
		id
		groupId
		isKey
		chatId
		fromUserId
		toUserIds
		whoCheckedIds
		ukm
		iv
		encryptedMessage
		sig
		secretAttachmentIds
		ikPub
		ekPub
		usedOpk
		createdAt
		updatedAt
	}
}
