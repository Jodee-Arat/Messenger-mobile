query GetSharedSecretKey($chatId: String!) {
	getSharedSecretKey(chatId: $chatId) {
		id
		groupId
		createdAt
		updatedAt
		ikPub
		chatId
		fromUserId
		toUserId
		ekPub
		usedOpk
		ukm
		iv
		encryptedKey
		sig
	}
}
